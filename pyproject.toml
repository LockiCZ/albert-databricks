[project]
name = "albert-databricks"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
    "pytest>=8.0",
    "pyspark>=3.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src/transforms", "src"]
